<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!--base de datos morelos LOCAL-->
        <property name="hibernate.connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/spring</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- JDBC connection pool (usa la integrada en Hibernate) -->
        <property name="connection.pool_size">50</property>

        <!-- Automatic session context managment -->
        <property name="current_session_context_class">thread</property>

        <!-- Imprime en salida estandar las sentencias SQL efectuadas por Hibernate -->
        <property name="show_sql">true</property>

        <property name="hbm2ddl.auto">update</property>

        <!-- Mapeos de Entitys -->
        <mapping class="mx.nexsol.entity.proyectos.Proyecto"/>
        <mapping class="mx.nexsol.entity.proyectos.CasoPrueba"/>
        <mapping class="mx.nexsol.entity.proyectos.PasoCasoPrueba"/>
        <mapping class="mx.nexsol.entity.proyectos.Funcionalidad"/>
        <mapping class="mx.nexsol.entity.catalogos.CatComplejidad"/>
        <mapping class="mx.nexsol.entity.comun.Usuario"/>
        <mapping class="mx.nexsol.entity.comun.UsuarioRol"/>
        <mapping class="mx.nexsol.entity.catalogos.CatPais" />
        <mapping class="mx.nexsol.entity.catalogos.Empresa"/>
    </session-factory>
</hibernate-configuration>
